using RestaurantProject.Domain.Models;
using RestaurantProject.Domain.Util;
using System.Threading.Channels;

namespace Testapp {
    public class Program {
        static void Main(string[] args) {
            //Table table = new Table(2, 5);
            //Table otherTable= new Table(1,5);
            //List<Table> tables = new List<Table>() { table, otherTable };
            //Restaurant restaurant = new("Test", new("Gent"), CuisineType.French, "test@test.com", "0478161421", new(), tables);

            //User user = new("Bert", "bert@test.be", "0478161421", new Location("Gent"));

            //try {
            //    DateTime curdate = DateTime.Now;
            //    Reservation reservation = new(restaurant, user, 4, curdate, new TimeOnly(17, 30));
            //    restaurant.Reservations.Add(reservation);


            //    Reservation reservation2 = new(restaurant, user, 4, curdate, new TimeOnly(17, 30));
            //    restaurant.Reservations.Add(reservation2);

            //    restaurant.Reservations.ForEach(r => Console.WriteLine(r));

            //}
            //catch(Exception ex) {
            //    Console.WriteLine(ex.Message);
            //}
        }
    }
}